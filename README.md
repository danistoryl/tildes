# FCL Language Interpreter

Modern C11 interpreter with lexer, parser, AST, VM, GC, and CLI.

## Build

```bash
make
```

## Run

```bash
./bin/interpreter test/test.lang
./bin/interpreter --repl
./bin/interpreter --help
```

## Test

```bash
make test
```

## Commit to GitHub

```bash
make commit
```
